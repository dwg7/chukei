# Decisions Log

ADR-lite log for this project. English per `CLAUDE.md`'s language convention. Append new decisions at the top, oldest at the bottom, following the pattern that worked well in the sibling `staccato_un_opengis` presentation project.

---

### D25 — Exclude `gsjgeomap_*`/`d1-no*` from Chukei's embedded catalog (2-week plan, Area 3)
**Date**: 2026-08-28
**What**: Added `CHUKEI_NOISE_ID_PREFIXES = ['gsjgeomap_', 'd1-no']` to `scripts/build-chukei-prompt.mjs`, applied in `isNoise()` alongside (not replacing) the GENNAI-inherited `NOISE_ID_PREFIXES`. Per the approved 2-week-plan analysis (`/Users/hfu/.claude/plans/cuddly-hugging-origami.md`, Area 3): `gsjgeomap_*` (866 entries, 51.2%) and `d1-no*` (175, 10.4%) are individual tile-sheet leaves GSI's own upstream `layers.txt` enumerates in bulk (confirmed against `~/layers-martin`'s `build_catalog.rb`), not something Chukei's non-technical Hokkaido audience plausibly asks for by name. Spot-checked `d1-no*`'s 3 Hokkaido entries (Sapporo x2, Hokkaido-wide, all routine periodic aerial-photo coverage, none tied to a specific event) before including it — the "current aerial photo" case Chukei's audience would actually want is already covered by the general-purpose `airphoto` layer. Rebuilt: 1690 → 649 layers-martin entries (74% size reduction). Verified: zero remaining `gsjgeomap_`/`d1-no` ids, the Hokkaido Iburi-earthquake landslide layer (`QUERY_EXAMPLES.md` case 1) still present, all 49 `fgd_update_*` entries still present (deliberately not excluded, next point).
**Why excluded these two but not others considered**: the date-prefixed one-off disaster-response orthophotos (e.g. `20180906hokkaido_*`, 265 entries) are numerically large too but genuinely useful and event-specific — explicitly kept, per the plan's own negative finding. `fgd_update_*` (49 entries, not thematic layers at all — "◯月更新" update-schedule markers) was hfu's explicit call to keep, "使用される可能性がある" (might still be used) — overriding this session's original recommendation to exclude it; deferred entirely to hfu's judgment per the plan's criterion 3 (reversible/low-risk changes favor the domain expert's call over a mechanical count).


### D24 — Pre-release audit: fixed `FEEDBACK_FORM.md` drift from the live form, removed the resolved `SPICCATO_STYLE_URL_REQUEST.md`
**Date**: 2026-08-24
**What**: hfu flagged that internal release is tomorrow; did a pre-release pass rather than waiting for problems to surface live. Re-read all of `CHUKEI_PROMPT.md` end-to-end (non-catalog sections) after the D16–D23 run of surgical edits — no bugs or dangling cross-references found. Opened the actual live feedback form (`https://forms.cloud.microsoft/r/X8VyNySW5s`) in a browser rather than trusting the D21 "confirmed still accurate" note secondhand, and found it had drifted from `FEEDBACK_FORM.md`'s documented 3-field design: field 2's options are `○期待どおり / △一部違う / ×違う`, not `はい/いいえ/一部` as drafted, and a 4th optional field ("追加コメント...") exists that was never in the spec. `FEEDBACK_FORM.md` rewritten to match the live form exactly (now the acknowledged source of truth). Deleted `SPICCATO_STYLE_URL_REQUEST.md` — it said its own job was done once D17/D18 resolved the request, and that resolution is already fully captured in `DECISIONS.md`, so keeping it around past release read as confusing (an unrelated future reader could mistake it for a still-open ask). `README.md`'s "Status" line updated (was still "Early setup"); its "Deploying to Gennai" section now explicitly says to re-run `npm run build` right before deploying, not rely on whatever was last committed, since `hfu/layers-martin` updates daily.
**Why**: The gap between "documented design" and "what's actually live" is exactly the kind of thing that's cheap to catch by looking and expensive to discover from a confused pilot user's feedback-form answer not matching what the docs say to expect. Same standard already applied to spiccato's `rstyle`/`ostyle` (D18: verified directly rather than taking the report on faith) — worth applying it here too, given the release is imminent rather than hypothetical.

### D23 — Strip CHUKEI_PROMPT.md's own title/preamble so the file is directly paste-ready
**Date**: 2026-08-24
**What**: Per hfu's observation, removed everything in `scripts/build-chukei-prompt.mjs`'s `buildPrompt()` template above `## あなたはStaffである` — the title, the fork-lineage paragraph, the department-context sentence, the "auto-generated, don't hand-edit" note, and the version-tag/feedback-link rationale paragraphs. `CHUKEI_PROMPT.md` now starts directly with the Staff role definition. Fixed two internal cross-references ("本ファイル冒頭に記載の...") that pointed at the now-removed header, rewording them to just point at the literal value shown immediately above in the same section. The removed content wasn't deleted from the project, just relocated to where it already substantively lived: `README.md` (fork lineage, what Chukei is), `DECISIONS.md` D5/D6/D9/D21 (version-tag and feedback-link rationale), and a new comment block at the top of `build-chukei-prompt.mjs` itself (the "auto-generated" note, now explicitly stated as a comment rather than in-band text, plus a note explaining *why* the generated file has no header).
**Why**: hfu's point is that this content is genuinely useful to a human deciding whether to trust and adopt Chukei, but has no operational role in the text that actually gets pasted into Gennai's saved-system-prompt box — and it had grown much longer than GENNAI_PROMPT.md's own terser equivalent (fork lineage, version tag rationale, feedback link rationale all accumulated over D9/D21/D22). With it gone, viewing the raw file on GitHub and pasting the whole thing into Gennai *is* the entire deployment step — no manual trimming first. This matches the project's own recurring preference for a low-friction, one-step experience (the terse response format itself, the single-form feedback loop) applied one level up, to the deployment step rather than just the response format.

### D22 — Distinguish direct matches from best-effort substitutes with a "近い" prefix, per GitHub issue #2
**Date**: 2026-08-24
**What**: Implemented `dwg7/chukei` issue #2 (hfu) exactly as specified: the success-response link text changes from `[地図](<link>)を用意しました。` to `近い[地図](<link>)を用意しました。` whenever Chukei can't be confident the result directly matches what was asked. The issue's six named triggers (no facility-point data → nearby aerial photo; requested map type missing → similar thematic map; requested era missing → nearest available year; only partial coverage of the target; catalog description doesn't fully specify content; uncertain whether the target area is within a layer's coverage) were added verbatim as a bulleted list in `scripts/build-chukei-prompt.mjs`'s "応答フォーマット" section, with the issue's explicit acceptance-criteria line "building a link at all doesn't by itself count as a direct match" carried over too. As a side effect, adopted the issue's exact wording `[地図](<link>)を用意しました。` (link before verb) for the direct-match case, replacing D21's slightly different word order (`地図を用意しました。[地図](<link>)。`) — issues #1 and #2 both specify the link-first order literally and consistently, so conformed to it rather than continuing to diverge. `CHUKEI_VERSION` bumped to `ちゅうけい2026-08-24a` (same-day suffix, D5's scheme, second substantive revision today). Regenerated `CHUKEI_PROMPT.md`, added a Hokkaido-flavored "近い" example (旭川市役所の場所, falling back to `airphoto` since no facility-point data exists in this catalog) to the prompt's own "## 例" section. Updated `QUERY_EXAMPLES.md`: reclassified all 12 dry-run cases as direct or "近い" — cases 9 (北海道の活断層), 10 (厚真町の被災前後比較), and 11 (江別市の土地条件図) turn out to be exactly the "近い" scenario, which is notable because those three were already flagged back in D19 as coverage/best-effort risk cases *before* "近い" existed as a mechanism to signal that to the user — the dry-run set ended up doubling as a ready-made acceptance-test set for this issue. Updated `TALK_PREP.md` (new Q&A entry) and `FEEDBACK_FORM.md` (placeholder text) to match.
**Why**: Implementing exactly what the issue specified (format, trigger list, acceptance criteria, examples) rather than re-deriving a competing design, matching the same approach taken for issue #1 (D21) — this was already a deliberated, example-backed proposal, not an open design question. The convergence with D19's dry-run findings is worth noting explicitly in `QUERY_EXAMPLES.md` because it's independent validation that the trigger list is well-calibrated to real cases this catalog actually produces, not just hypothetical ones.

### D21 — Embed the feedback form link in every Chukei response, per GitHub issue #1
**Date**: 2026-08-24
**What**: Implemented `dwg7/chukei` issue #1 (hfu): the live Microsoft Forms feedback link (`https://forms.cloud.microsoft/r/X8VyNySW5s`, built by hfu directly in Forms — `FEEDBACK_FORM.md`'s field spec was not used as a step-by-step build script, but its field design is unaffected and confirmed still correct) is now embedded as `[フィードバックする](...)` in **every** Chukei response, success or not-found, per the issue's proposed format. This is a deliberate departure from D6/D9's "collapse every response to exactly one line" principle — the issue's own reasoning (quoted into `scripts/build-chukei-prompt.mjs`'s "応答フォーマット" section) is that during the pilot, learning from feedback matters more than brevity; the format is still a fixed 3-paragraph template, not free-form, so it stays predictable and un-narrated. `CHUKEI_VERSION` bumped to `ちゅうけい2026-08-24` (first real content revision since the 2026-08-22 draft) and a new `FEEDBACK_FORM_URL` build-time constant added alongside it, both in `scripts/build-chukei-prompt.mjs`. `CHUKEI_PROMPT.md` regenerated. `TALK_PREP.md` and `QUERY_EXAMPLES.md` updated to match the new format; `TALK_PREP.md`'s demo item 5 ("show the feedback form separately") is now redundant and replaced with a note that every demo query already carries the link. `FEEDBACK_FORM.md` updated to record the form as live and to describe the new primary distribution path (in-response link) superseding the original "reach the form via the talk slide" plan.
**Why**: hfu filed this as a considered GitHub issue (not an inline chat request) with explicit rationale — makes feedback collection part of normal usage rather than a separate step, reduces friction, and (per the issue) should work the same way for a future Copilot deployment as for Gennai, since it's just prompt text rather than anything Gennai-specific. Implementing exactly what was proposed (rather than re-deriving a competing design) respects that this was already a deliberated decision, not an open question.

### D20 — Volcano land-condition/basic-map queries always prefer stars-optgeo's `vlcm`/`vbm` style over layers-martin's per-volcano `vlcd_*` data
**Date**: 2026-08-22
**What**: Resolved the cross-catalog ambiguity D19 flagged (有珠山: `vlcd_usu` vs. `vlcm` style) per hfu's explicit instruction. Added a new bullet to `scripts/build-chukei-prompt.mjs`'s stars-optgeo section: for "火山土地条件図"/"火山基本図" requests, `rstyle=vlcm`/`ostyle=vbm` is always preferred over catalog 1's `vlcd_<volcano>` series, even when a specific volcano is named — as long as that volcano falls within stars-optgeo's 道南〜道央 coverage (`bbox` may still zoom to the named volcano; only the source of the styled data changes). `vlcd_<volcano>` in layers-martin is now explicitly framed as the fallback for volcanoes outside that coverage (e.g. 十勝岳, 雌阿寒岳— 道東・道北寄り). Regenerated `CHUKEI_PROMPT.md`. Updated `QUERY_EXAMPLES.md` cases 2 (有珠山) and 4 (北海道駒ヶ岳, 渡島半島/道南) to use the stars style; case 3 (十勝岳) is unchanged since it falls outside stars' coverage; case 5 (樽前山の赤色立体地図) is unaffected since 赤色立体地図 is a different product than 火山土地条件図/基本図 and isn't covered by `vlcm`/`vbm` at all.
**Why**: hfu's explicit call — the stars-optgeo styles are GSI-official-legend, fully symbolized finished products (the same reasoning already established in D6/D9 for why Chukei prefers finished products over raw data generally), so defaulting to them whenever they're available is more consistent with Chukei's whole design philosophy than leaving the choice ambiguous per-query.

### D19 — Write `QUERY_EXAMPLES.md`: a dozen realistic Hokkaido-grounded queries, dry-run tested
**Date**: 2026-08-22
**What**: Added `QUERY_EXAMPLES.md` — 12 plain-language queries grounded in real Hokkaido geography, administration, and disaster history (2018 Iburi earthquake/Atsuma landslides, named Hokkaido volcanoes with actual `vlcd_*` entries, 石狩川 flood risk, Sapporo DID, 千島海溝 tsunami risk for the 十勝・釧路 coast, active-fault and land-condition coverage gaps, and one deliberately out-of-scope administrative-statistics question) rather than generic placeholder questions — for realism in the talk/pilot materials, per hfu's request. Every `source_id`/`style_id` referenced was grep-verified against the actual generated `CHUKEI_PROMPT.md`. Then dry-run tested all 12 the same way the `TALK_PREP.md` demo queries were (D-numbered dry-run precedent), producing the actual candidate response for each. Two findings surfaced: (1) query 2 (有珠山) exposed an undocumented ambiguity — `vlcd_usu` (layers-martin, single-volcano data) and `vlcm` (stars-optgeo's regional style, D17/D18) both plausibly answer "有珠山の火山土地条件図", and `CHUKEI_PROMPT.md`'s existing same-name-candidate guidance is scoped to catalog 1 only, not cross-catalog — flagged for hfu's judgment, not acted on (no prompt change made, low real-world harm either way); (2) confirmed queries 9 (北海道の活断層) and 11 (江別市の土地条件図) exercise the already-documented "限定的なカバレッジ" constraint for real — the response format's one-line design means Chukei never narrates this to the user, so a pilot report of "the map came back empty" for these should be checked against known coverage gaps before assuming a prompt bug.
**Why**: hfu's explicit ask — the three demo queries in `TALK_PREP.md` (D14) are useful for a controlled 8-minute demo but don't carry the same realism/credibility as questions that visibly come from actual departmental concerns (a specific quake, a specific volcano, a specific coastal risk). Dry-run testing them the same way earlier demo queries were tested (rather than just listing them) catches the same class of problem the TALK_PREP.md dry-run caught (D14/D16's dry-run precedent) before real Gennai access is available.

### D18 — Verified `rstyle`/`ostyle` live on production spiccato, directly (not just relayed)
**Date**: 2026-08-22
**What**: After hfu had the spiccato session push D17's change to `main`, this session verified it directly against the real, public `https://dwg7.github.io/spiccato/` — not just relayed the other session's report. First attempt (via the Browser tool's `navigate`) showed the form fallback for *both* the new `rstyle`/`ostyle` link and a known-good pre-existing `req`/`opt` link, which looked like a possible regression; root cause turned out to be the automation tool's `navigate` not triggering spiccato's initial hash-read the way a real browser load does — confirmed by checking `location.hash` (correctly populated in both cases) and finding that an explicit `location.reload()` after navigating made both render correctly. With that in hand, re-verified the actual demo query 3 link (`rstyle=vlcm|火山土地条件図&ostyle=vbm|火山基本図`): panel shows "火山土地条件図、火山基本図 を表示。", required style checked, optional style unchecked (matches documented optional-hidden-by-default behavior), no console errors (only benign MapLibre geolocation/hillshade warnings, matching what the spiccato session's own report described). `TALK_PREP.md` updated to drop the "confirm the push landed" caveat — it's confirmed, not just expected.
**Why**: hfu's report in D17 was itself already careful (tests + a "production-equivalent build" check), but a same-day, config-dependent chain (implement → commit → push → CI redeploy → live) has several places a talk-day demo could quietly fail even when every upstream step succeeded — confirming against the actual public URL this session will drive costs little and removes a specific, plausible failure mode ("looked done on spiccato's end but GitHub Pages hadn't actually redeployed yet," or a caching issue) right before the one deadline that matters (the lightning talk demo).

### D17 — spiccato added `rstyle`/`ostyle` URL params; Chukei's prompt updated to drop the YAML-dump path entirely
**Date**: 2026-08-22
**What**: The spiccato Claude Code session (via `SPICCATO_STYLE_URL_REQUEST.md`, D16) reported the fix was a small, additive change: `StyleRef` turned out to have the exact same shape as `LayerRef`, and `resolveStyles` already walked the same single `active_catalogs` array `resolveLayers` does, so no new constraint was needed. Added `rstyle=`/`ostyle=` params to `src/shorthand.ts` (same `style_id[|label]` wire format as `req`/`opt`); `mcp/src/linkBuilder.ts` and `openweb/main.ts` needed no changes since they already passed `required_styles`/`optional_styles` through and now get a `#q=` link instead of the `#m=` fallback automatically; GENNAI_PROMPT.md's own vlcm/vbm guidance was switched from YAML to a `#q=` link too. Tests added/updated (110+13 passing), verified live against `stars.optgeo.org` in a production-equivalent build (`rstyle=vlcm|火山土地条件図&ostyle=vbm|火山基本図` resolves and labels correctly, no console errors). Committed locally on spiccato's `main` (`43f26a0`, `0a355d7`), then pushed to production per hfu's go-ahead in this session.

Chukei-side follow-up (this session): updated `scripts/build-chukei-prompt.mjs` — the "やりとりの形" URL template now includes `rstyle=`/`ostyle=` alongside `req=`/`opt=`; the "応答フォーマット" section's special-case "YAMLでしか表現できない場合" success format is **removed entirely** (no longer needed — every successful response, layer or style, now collapses to the same one-line link format); the stars-optgeo catalog section's vlcm/vbm guidance now shows a `#q=` link example instead of a YAML block, plus a new explicit "同名の罠" caution (`vbm`/`vlcm` exist as both a raw `source_id` and a `style_id` in the same catalog — easy to conflate, `rstyle`/`ostyle` must be used for the "give me the finished thematic map" case, not `req`/`opt`). Regenerated `CHUKEI_PROMPT.md` via `npm run build`. `TALK_PREP.md`'s demo query 3 is un-held. Version tag left unbumped (`ちゅうけい2026-08-22`) since nothing has been deployed to Gennai yet — D5's bump trigger is "revised *and* re-deployed," and there's been no real deployment to distinguish this revision from.
**Why**: This resolves D16 in favor of option (b) (fix spiccato) over (a) (drop styles from Chukei) — exactly the outcome hfu wanted, achieved without sacrificing the vlcm/vbm pre-symbolized-style feature. Removing the YAML case entirely (rather than leaving it as unused dead code) matches this repo's stated preference for a narrow, lean fork (D6) — now that no code path needs it, keeping it around would just be a stale branch nobody exercises.

### D16 — Flag the style_id YAML-dump response as unfriendly for Chukei; request a spiccato-side URL fix before falling back to dropping styles
**Date**: 2026-08-22
**What**: A dry-run simulation of `CHUKEI_PROMPT.md`'s demo query 3 ("北海道の火山土地条件図を見たい") surfaced that `required_styles`/`optional_styles` responses (the `vlcm`/`vbm` case) can only be rendered today by dumping a raw Map Intent YAML block and telling the user to paste it into spiccato's form — there's no `#q=` URL-scheme equivalent for `style_id` the way `req`/`opt` already cover `source_id`. hfu judged this genuinely bad for Chukei's audience (non-technical staff), even though it's fine for spiccato's own `STAFF_PROMPT.md` audience. Wrote `SPICCATO_STYLE_URL_REQUEST.md` — a self-contained prompt for the separate Claude Code session working in `dwg7/spiccato` — proposing an additive URL param (e.g. `rstyle=`/`ostyle=`) so style_id references can also become a one-line link, with an explicit fallback stated in the same request: if that's not straightforward, Chukei should stop emitting style_id-based responses entirely and only ever use `req`/`opt` against individual layers. Demo query 3 in `TALK_PREP.md` is marked **held** pending that reply, with the other three demo queries as the fallback set. hfu will deliver the prompt to that other session manually and report results back.
**Why**: Matches the terse-response design goal from D6/D9 — a raw YAML dump directly contradicts Chukei's "collapse every response to one short, friendly line" promise, and for a pilot whose main risk is a bad first impression (per D9's original rationale), handing a non-technical user a config file to paste somewhere is exactly the kind of experience that erodes trust fast. Asking spiccato to close the URL-scheme gap (rather than immediately dropping styles from Chukei) was hfu's explicit preference — preserving the vlcm/vbm pre-symbolized-style feature is worth a small upstream ask if the fix is cheap, and only worth sacrificing if it isn't.

### D15 — Copilot users get pointed to `STAFF_PROMPT.md`; the "friendly," pilot-ready build-out stays Gennai-only
**Date**: 2026-08-22
**What**: Decided not to build a Copilot-specific fork of Chukei for this project or the lightning talk. Instead: (1) Copilot users are pointed at `hfu/layers-martin`'s existing `STAFF_PROMPT.md` (the fetch-based, more technical sibling of GENNAI_PROMPT.md/CHUKEI_PROMPT.md) — no new engineering, it already exists; (2) the "friendly," pilot-ready build-out (D6's terse response format, D13's feedback form, D14's talk prep) stays scoped to 源内 only; (3) framing for the talk (see updated `TALK_PREP.md` Q&A): this is **not** "there's no Copilot option," it's "the Copilot option has a more technical flavor (`STAFF_PROMPT.md`) than the one built for this pilot" — hfu explicitly asked to preserve this distinction rather than imply Copilot users are unsupported; (4) also noted for accuracy: nothing about `CHUKEI_PROMPT.md`'s content is actually Gennai-exclusive — it's static instructions text and would likely work pasted into Copilot as-is, since the "no internet access" constraint that shaped its embed-everything catalog design is a property of Gennai's environment, not a limitation Copilot happens to share. That path is simply untested and unpromised for this pilot, not technically blocked — worth being precise about if asked, per hfu's "説明戦略はよく考えよう" note.
**Why**: Timeline (Gennai testing itself is already blocked until ~2026-08-24, and a genuine Copilot Studio build — Actions/plugins wired to live-fetch the catalog, per the STAFF_PROMPT.md-style architecture this would need, see the essential-difference discussion this decision follows from) is separate engineering, not a redeploy, and there isn't time before the talk; tone fit (`TALK_PREP.md`'s "this is rough, still figuring out what's next" framing suits an honest "not built for this yet" answer better than a rushed parallel build); an unconfirmed prerequisite (whether the department has Copilot Studio custom-agent build permissions at all hasn't been checked, so committing engineering time before that's known would be premature).

### D14 — Write `TALK_PREP.md`: lean talk outline, break-it demo, Q&A distilled from `JUSTIFICATION.md`
**Date**: 2026-08-22
**What**: Added `TALK_PREP.md` — a talk outline (5 min), demo script (8 min), and Q&A prep for the lightning talk. Talk outline deliberately omits any architecture/Staccato-spec explanation per hfu's direction; structure is hook → one-sentence description → name-pun aside → "this breaks, play with it" message → straight into demo. Demo script sequences the three existing `CHUKEI_PROMPT.md` examples (plain layer query, multi-layer with labels, style YAML) plus a **deliberate fourth query designed to trigger the terse "not found" response** — chosen so the "it's okay to break it" message is demonstrated live, not just stated. Q&A section is a condensed, talk-ready version of `JUSTIFICATION.md`'s argument (including its honest "not officially ratified" caveat verbatim) plus answers for two questions `JUSTIFICATION.md` doesn't cover: the open Copilot-scope question (answered honestly as undecided, not glossed over) and data-security/provenance.
**Why**: hfu asked for a plan for the remaining work and specifically flagged `JUSTIFICATION.md` as prepared Q&A material to reference. Condensing it into short spoken-ready answers now (rather than expecting hfu to re-derive talk-appropriate phrasing from the fuller document under Q&A pressure) is lower-risk than leaving that translation for the moment itself. The deliberate break-it demo query exists because *showing* graceful failure lands the "play with it" message harder than *saying* it — matches the same principle already applied in D9's design of the terse not-found response itself (a bad first experience during the pilot week costs more trust than a slow rollout).

### D13 — Feedback form: anonymous only, three fields, pilot framing lives on the talk slide not the form
**Date**: 2026-08-22
**What**: Wrote `FEEDBACK_FORM.md`, the Microsoft Forms question spec (not yet built in Forms itself). Resolves the two open questions left in D4/HANDOVER.md: (1) anonymous only, no name/section field at all; (2) the "this is a pilot, report odd results" expectation-setting note goes on the lightning talk's slide, not inside the form. Settled on three fields: paste Chukei's full response text (not just the link, so the link-less "not found" case is still traceable via its version tag), the required yes/no/partially structured field from D4, and one optional free-text field for original intent + impression combined into one (not split into two, to keep friction low). Also wrote MS Forms-specific setup notes (turn off "Record name", don't require sign-in) so the anonymity decision actually holds at the tenant-settings level, not just in the visible field list.
**Why**: hfu's explicit choice on both open questions (asked via AskUserQuestion this session) — anonymity was picked to prioritize getting candid criticism during the pilot's most fragile week over the ability to follow up with a specific respondent; the expectation-setting note went to the slide because that's the moment staff actually decide whether to try Chukei at all, whereas by the time someone opens the feedback form they're already past that decision. Pasting the whole response (not just the link, per D4's original phrasing) was necessary once D6/D9 gave the terse "not found" response no link at all — a link-only field would silently fail to capture the query that matters most for iterating on Chukei's biggest current risk (bad first impressions during the pilot, per the original D9-adjacent HANDOVER note).

### D12 — Write `JUSTIFICATION.md`: the logical case for hosting Chukei under `dwg7`
**Date**: 2026-08-22
**What**: Added `JUSTIFICATION.md`, a standalone document laying out why Chukei legitimately belongs under the `dwg7` org — grounded in DWG7's own published vision ("keep web maps open") and mission ("test new technologies for future geospatial operations"), its explicit charter invitation to "AI systems themselves," Chukei's direct forked lineage from `dwg7/spiccato`, hfu's standing as a DWG7 member, and the public commitment to "a smarter staff" made in the 2026-08-19 UN Open GIS Featured Talk that Chukei fulfills. Closes with an explicit honest caveat: this is member-driven work in DWG7's shared space, not an officially ratified DWG7 deliverable — matching how `staccato-spec` is already positioned, and matching that project's own Q&A prep on the same question.
**Why**: hfu's explicit instruction, prepared to hand to this session as ready material — for the lightning talk Q&A, for a GSI colleague asking "wait, is this an official DWG7 thing?", or for future justification of the repo's placement without needing to re-derive the argument from scratch each time.

### D11 — License as CC0 1.0 Universal
**Date**: 2026-08-22
**What**: Added `LICENSE` (CC0 1.0 Universal, fetched verbatim from creativecommons.org) and a `## License` section in `README.md`.
**Why**: hfu's explicit instruction, matching the license already used across the other `dwg7` repos (`spiccato`, etc.) and consistent with D2's "keep this kind of work open" rationale for making the repo public in the first place.

### D10 — Implement `scripts/build-chukei-prompt.mjs`, fork of `build-gennai-prompt.mjs`
**Date**: 2026-08-22
**What**: Wrote `scripts/build-chukei-prompt.mjs`, reusing `build-gennai-prompt.mjs`'s fetch/format/noise-filtering logic wholesale (per D7) and templating in the response-format section drafted by hand in D9. Added a `CHUKEI_VERSION` constant at the top of the script (currently `ちゅうけい2026-08-22`) as the single source of truth for the version tag baked into every response (D5) — bump this constant, then re-run, to produce the next revision. Added a minimal `package.json` with a `build` script (`npm run build`) that runs it. Ran it once against the live `hfu/layers-martin` and `stars.optgeo.org` catalogs to regenerate `CHUKEI_PROMPT.md` (1690 layers-martin + 8 stars-optgeo entries as of this run) — replacing the D9 hand-forked draft with build-script output, matching GENNAI_PROMPT.md's own "auto-generated, don't hand-edit" convention. `CHUKEI_PROMPT.md`'s header text now says it's auto-generated again, per the TODO left in D9/HANDOVER.md.
**Why**: A hand-maintained catalog snapshot goes stale the same way any manually-copied one does (D7's original rationale) — now that this exists, redeploying Chukei to Gennai after a `layers-martin` catalog update, or after a prompt-text revision, is a single `npm run build` away rather than a repeat of the D9 manual copy-and-edit.

### D9 — Draft `CHUKEI_PROMPT.md` as a hand-forked copy, not build-script output, for now
**Date**: 2026-08-22
**What**: Created `CHUKEI_PROMPT.md` by copying `reference/GENNAI_PROMPT.md.snapshot-2026-08-21.md` and editing in place: replaced the header/provenance block, added a new `## 応答フォーマット(ちゅうけい)` section (placed right after `## やりとりの形`) defining the three terse response templates (success-with-link, success-with-YAML, not-found), pointed the two spots in the inherited text that used to say "add a one-line description" or "state not-found plainly" at that new section instead, and updated the closing `## 例` section to show the new one-line response wrapped around each existing link/YAML example. Catalog listings, anti-fabrication rules, bbox-guessing policy, and Cartographer-capability notes are untouched. Version tag baked in as the literal string `ちゅうけい2026-08-22` (today, per D5) — will need bumping at each future revision.
**Why**: Matches D6's decision to keep this a narrow fork. `scripts/build-chukei-prompt.mjs` doesn't exist yet (see D7), so there's no automated way to regenerate this file from a live catalog fetch today; hand-forking the pinned reference snapshot is the fastest way to get a testable draft in front of hfu before the lightning talk, and the file's own header now says plainly that it's hand-forked pending the build script, so nobody mistakes it for auto-generated output the way GENNAI_PROMPT.md's header would imply.

### D8 — Pin a snapshot of spiccato's GENNAI_PROMPT.md and its build script in `reference/`
**Date**: 2026-08-21
**What**: Copied `dwg7/spiccato`'s `GENNAI_PROMPT.md` and `scripts/build-gennai-prompt.mjs` (as of that date) into this repo's `reference/` folder, suffixed `.snapshot-2026-08-21`.
**Why**: Chukei is a direct fork of this prompt and its generation mechanism. Pinning a snapshot means a fresh session can read the exact structure being forked from without a network round-trip to GitHub, while the filename makes clear it's a point-in-time copy, not a live sync — check spiccato's `main` branch if working on anything the snapshot might not cover.

### D7 — Reuse spiccato's catalog-embedding build script rather than hand-copying the catalog
**Date**: 2026-08-21
**What**: Plan to fork `scripts/build-gennai-prompt.mjs` into `scripts/build-chukei-prompt.mjs`, reusing its fetch/format/noise-filtering logic against the same `hfu/layers-martin` and `stars.optgeo.org` catalogs. Not yet implemented.
**Why**: `hfu/layers-martin` updates daily (~1,873 layers as of this writing). A hand-copied catalog snapshot would go stale the same way a manually-maintained one always does. Reusing the proven fetch/format logic avoids re-deriving it and keeps Chukei's catalog listing correct by construction.

### D6 — Chukei's prompt is a narrow fork of GENNAI_PROMPT.md, not a rewrite
**Date**: 2026-08-21
**What**: The only planned change to the inherited prompt content is the final-response-format instruction — collapse it from "build a link + add a one-line description of what it shows" down to a fixed terse format: `地図を用意しました。[地図](<link>)。ちゅうけい<version>`. Everything else (catalog embedding, anti-fabrication rules for `source_id`/`style_id`, the two-catalog structure, bbox-guessing policy, Cartographer-capability notes) is inherited unchanged.
**Why**: GENNAI_PROMPT.md already encodes a lot of hard-won design (see its own D13/D15-equivalent history around a mistaken size-driven catalog trim that was reverted after real testing, and the documented `lcmfc2`/`lcmfc2_1` fabrication incident). Re-deriving that from scratch would be wasted and risky. The only genuinely new requirement is the end-user response format for this specific, more non-technical pilot audience.

### D5 — Version tag is a literal build-time constant, never AI-computed
**Date**: 2026-08-21
**What**: The `ちゅうけい2026-09-02`-style tag appended to every response is a hardcoded string baked into the prompt at build/publish time (bumped manually or by a release step whenever the prompt is revised and redeployed to Gennai), with an optional same-day suffix (`...02a`, `...02b`) for rapid iteration days. Gennai is never asked to generate the date itself.
**Why**: Two reasons. First, the base prompt already tells the model not to trust its own sense of the current date for `provenance.generated_at` — the same caution applies here. Second, the entire purpose of the tag is to let a piece of user feedback be traced back to the exact prompt revision that produced it; that only works if the tag is fixed per-deploy, not computed per-response.

### D4 — Feedback form: paste the generated link, not the original question; add one structured field
**Date**: 2026-08-21
**What**: Design the Microsoft Forms feedback channel to primarily ask staff to paste the Chukei-generated spiccato link (which losslessly encodes the resolved Map Intent), rather than retype their original question. Add one structured multiple-choice field ("Did this match what you expected? — yes / no / partially") alongside a free-text impressions field.
**Why**: Retyping a question is real friction for busy staff and this pilot's success depends on actually getting feedback. The link is already the artifact that matters most for debugging a mismatch, and pasting it is near-zero-effort. A structured field makes it possible to track improvement over prompt revisions quantitatively, not just anecdotally.

### D3 — Language split: English docs/code, Japanese generated output
**Date**: 2026-08-21
**What**: All repo documentation, code, comments, commit messages, and this decisions log are written in English, matching `dwg7/spiccato`'s treatment as an international project. The Chukei prompt text itself, and every response it produces, is Japanese — its entire audience is Japanese-speaking GSI staff.
**Why**: hfu's explicit instruction. Keeps the `dwg7` org's documentation consistently readable internationally while keeping the actual product correctly localized for its real, single-language audience.

### D2 — Repo created public under `dwg7`, matching sibling repos
**Date**: 2026-08-21
**What**: `gh repo create dwg7/chukei --public`.
**Why**: Consistent with `dwg7/spiccato` and the DWG7 (Smart Maps) working group's stated vision of keeping this kind of work open. No sensitive data (no staff names, no actual survey content, no real feedback-form responses) is planned to live in this repo — only the prompt design and process documentation.

### D1 — Always operate as the `hfu` GitHub account, never `handygeospatial`
**Date**: 2026-08-21
**What**: Verified via `gh auth status` before creating the repo that only the `hfu` account was authenticated, and confirmed `hfu` is a member of the `dwg7` org before running `gh repo create`.
**Why**: hfu flagged that a past session mistakenly operated as `handygeospatial` instead of `hfu`. Recorded here as a standing check for any future repo/GitHub operation in this project: confirm `gh auth status` shows `hfu` as the active account first.
